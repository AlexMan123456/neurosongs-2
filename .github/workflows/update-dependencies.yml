name: Update Dependencies
on:
  workflow_dispatch:
  schedule:
  - cron: 0 21 * * 5

permissions:
  contents: write
  pull-requests: write

jobs:
  update-dependencies:
    if: ${{ github.repository == 'AlexMan123456/neurosongs-2' }}
    uses: AlexMan123456/github-actions/.github/workflows/update-dependencies.yml@c016c13a6227b8a31b5ddd866063bb5362873e70 # v3.0.1
    secrets:
      PAT_GITHUB: ${{ secrets.PAT_GITHUB }}
