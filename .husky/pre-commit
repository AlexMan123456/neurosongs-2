#!/bin/bash
set -e

if git diff --cached --quiet; then
  echo "No staged changes found."
  exit 1
fi

npm run format -- --no-color
npm run lint -- --no-color
npm test -- --no-color

git update-index --again
